export enum defaultValues {
  parentId = "body",
}

export const defaultBodyValue = {
  body: {
    childrens: [],
    id: "body",
    data: {
      component: "div",
      id: "body",
      parentId: "parent",
      child: "",
    },
  },
};
